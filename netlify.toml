[build]
  command = "npm run build"
  publish = "dist"

# Single page app: send every path to index.html so client routes
# and staff QR links like /r/tumi-maseko-7f3k resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
